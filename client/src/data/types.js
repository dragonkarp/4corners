//React DnD requires to specify the type of the item.
//to distinguish what kind of draggable item we are working with
// we will be using only one but later on if you

const ITEM_TYPE = "ITEM";

export default ITEM_TYPE;

